/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ImageModel } from '../../models/image-model';

export interface ApiImagePost$Json$Params {
      body?: {
'Image'?: Blob;
}
}

export function apiImagePost$Json(http: HttpClient, rootUrl: string, params?: ApiImagePost$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<ImageModel>> {
  const rb = new RequestBuilder(rootUrl, apiImagePost$Json.PATH, 'post');
  if (params) {
    rb.body(params.body, 'multipart/form-data');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ImageModel>;
    })
  );
}

apiImagePost$Json.PATH = '/api/Image';
